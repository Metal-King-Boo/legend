package com.example.legend;

public class ManaPool {
    int currentMana;
    int totalMana;

    ManaPool() { }

    ManaPool(int currentMP, int totalMP) {
        currentMana = currentMP;
        totalMana = totalMP;
    }
}
